const os = require("os");

console.log(os.cpus());

console.log("The platform name is", os.platform());

console.log("The platform name is", os.platform());

console.log("Host name is", os.hostname());
